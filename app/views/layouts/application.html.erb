<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "The Reserve" %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'custom', media: 'all' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&display=swap" rel="stylesheet">
</head>
<body>

  <header>
  <% if user_signed_in? %>
    <div class="logout-container">
      <%= button_to "Logout", logout_path, method: :delete, class: "logout-btn", data: { turbo: false } %>
    </div>
  <% end %>

  <div class="max-w-6xl mx-auto text-center">
    <h1 class="font-luxury tracking-wide">The Reserve</h1>
  </div>
</header>

  <main>
    <div class="content-container">
      <%= yield %>
    </div>
  </main>

  <footer>
    &copy; <%= Time.now.year %> The Reserve. All rights reserved.
  </footer>

</body>
</html>
