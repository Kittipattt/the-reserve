<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application.tailwind', media: 'all' %> <!-- Keep Tailwind CSS if needed -->
  <%= stylesheet_link_tag 'login', media: 'all' %>

  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;700&display=swap" rel="stylesheet">
  
  <%= stylesheet_link_tag 'login' %> <!-- Add this line to include custom CSS -->
</head>
<body class="bg-[#0b2e26] flex items-center justify-center min-h-screen px-6 py-12">

  <div class="login-box">
    <h1 class="login-heading">Welcome Back</h1>

    <%= form_with(url: login_path, method: :post, class: 'space-y-6') do |form| %>
      <div>
        <%= form.label :email, class: 'block text-base font-medium text-white mb-2' %>
        <%= form.text_field :email, required: true, class: 'w-full p-4 bg-transparent border border-yellow-400 rounded-lg text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400 text-lg' %>
      </div>

      <div>
        <%= form.label :password, class: 'block text-base font-medium text-white mb-2' %>
        <%= form.password_field :password, required: true, class: 'w-full p-4 bg-transparent border border-yellow-400 rounded-lg text-white placeholder-gray-300 focus:outline-none focus:ring-2 focus:ring-yellow-400 text-lg' %>
      </div>

      <div>
        <%= form.submit 'Login', class: 'w-full py-3 bg-yellow-400 text-[#0b2e26] font-semibold rounded-lg hover:bg-yellow-300 transition duration-300 shadow-md text-lg' %>
      </div>
    <% end %>

    <%= link_to 'Forgot password?', '#', class: 'text-sm text-white hover:text-yellow-300 mt-6 block text-center' %>
  </div>

</body>
</html>
