<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application.tailwind', media: 'all' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>
<body class="bg-gray-900">

  <div class="min-h-screen flex items-center justify-center">
    <div class="bg-gradient-to-r from-[#2C3E50] to-[#8E44AD] p-12 rounded-lg shadow-2xl w-full max-w-md">

      <h1 class="text-3xl font-semibold text-center mb-6 text-white">Login</h1>

      <%= form_with(url: login_path, method: :post, class: 'space-y-6') do |form| %>
        <div class="mb-4">
          <%= form.label :email, class: 'block text-sm font-medium text-white' %>
          <%= form.text_field :email, required: true, class: 'w-full p-4 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gold-500' %>
        </div>

        <div class="mb-4">
          <%= form.label :password, class: 'block text-sm font-medium text-white' %>
          <%= form.password_field :password, required: true, class: 'w-full p-4 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gold-500' %>
        </div>

        <div class="flex items-center justify-between">
          <%= form.submit 'Login', class: 'px-6 py-3 bg-gold-600 text-white rounded-md hover:bg-gold-700 font-semibold text-lg shadow-md' %>
        </div>
      <% end %>

      <%= link_to 'Forgot password?', '#', class: 'text-sm text-white hover:text-gold-500 mt-3 block text-center' %>
    </div>
  </div>

  <style>
    /* Tailwind customization for gold color */
    .bg-gold-600 {
      background-color: #FFD700;
    }
    .bg-gold-700 {
      background-color: #FFC107;
    }
    .focus:ring-gold-500 {
      box-shadow: 0 0 0 2px rgba(255, 215, 0, 0.6);
    }
  </style>

</body>
</html>
