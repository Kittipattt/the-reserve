<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reserve Room</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application.tailwind', media: 'all' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>
<body class="bg-gray-900">

  <div class="min-h-screen flex items-center justify-center">
    <div class="bg-gradient-to-r from-[#2C3E50] to-[#8E44AD] p-12 rounded-lg shadow-xl w-full max-w-6xl">

      <h1 class="text-4xl font-bold text-center mb-6 text-white tracking-wide">Reserve <%= @room.name %></h1>

      <div class="text-white opacity-75 text-center mb-6">
        <p><strong>Description:</strong> <%= @room.description %></p>
      </div>

      <%= form_with url: dashboard_path, method: :post, class: "space-y-6" do |form| %>
        <div class="mb-4">
          <%= form.label :check_in, "Check-in Date", class: "block text-white" %>
          <%= form.date_field :check_in, class: "w-full p-4 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gold-500", required: true %>
        </div>

        <div class="mb-4">
          <%= form.label :check_out, "Check-out Date", class: "block text-white" %>
          <%= form.date_field :check_out, class: "w-full p-4 border-2 border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-gold-500", required: true %>
        </div>

        <div class="mb-4">
          <%= form.submit 'Reserve Now', class: "px-6 py-3 bg-gold-600 text-white rounded-md hover:bg-gold-700 font-semibold text-lg shadow-md" %>
        </div>
      <% end %>

    </div>
  </div>

</body>
</html>
