<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag 'application.tailwind', media: 'all' %>
  <%= javascript_include_tag 'application', 'data-turbo-track': 'reload' %>
</head>
<body class="bg-gray-900">

  <div class="min-h-screen flex items-center justify-center">
    <div class="bg-gradient-to-r from-[#2C3E50] to-[#8E44AD] p-12 rounded-lg shadow-xl w-full max-w-6xl">

      <h1 class="text-4xl font-bold text-center mb-6 text-white tracking-wide">Welcome to your Dashboard</h1>

      <p class="text-xl text-center mb-6 text-white opacity-75">Welcome, <%= current_user.first_name %>!</p>

      <!-- Room Cards Grid with 3 rooms per row -->
      <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-3 xl:grid-cols-3 gap-6">
        <% if @rooms.present? %>
          <% @rooms.each do |room| %>
            <%= render 'room', room: room %>  <!-- Rendering the room partial -->
          <% end %>
        <% else %>
          <p class="text-center text-white opacity-75">No rooms available to reserve at the moment.</p>
        <% end %>
      </div>      
    </div>
  </div>

</body>
</html>
